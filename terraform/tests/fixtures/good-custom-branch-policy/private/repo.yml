good-custom-branch-policy-repo:
  description: "environment with a custom deployment branch policy"
  visibility: private
  environments:
    production:
      deployment_branch_policy:
        protected_branches: false
        custom_branch_policies: true
      branch_policies:
        - "release/*"
